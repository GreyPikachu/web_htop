apiVersion: v1
kind: Service
metadata:
  name: web-htop
  labels:
    app.kubernetes.io/name: web-htop
spec:
  selector:
    app.kubernetes.io/name: web-htop
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: telemetry
      port: 9999
      targetPort: telemetry
